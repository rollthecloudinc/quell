import { AttributeWidgetDirective } from './attribute-widget.directive';

describe('AttributeWIdgetDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeWidgetDirective();
    expect(directive).toBeTruthy();
  });
});
